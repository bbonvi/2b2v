The memory changes for this private pass. Include multiple focused actions when needed; do not collapse distinct user/self/maintenance updates into one vague note. Multiple new memories are appropriate only when they would affect different future decisions, subjects, or time windows. Add memory only when it changes what 2B says, does, avoids, trusts, remembers, or checks later after recent chat is gone. `important: true` means pinned scarce context that should almost always be present until changed or expired; do not use it for incident logs or emotional intensity. Do not mark incident logs, self-journal beats, rude messages, or latest conflict turns important. Prefer updating one existing overlapping row over adding another. If several rows say the same thing, consolidate by updating the best row and deleting obsolete duplicates.
