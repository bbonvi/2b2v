The memory changes for this private pass. Include multiple focused actions when needed; do not collapse distinct user/self/maintenance updates into one vague note. Add memory only when it changes what 2B says, does, avoids, trusts, remembers, or checks later after recent chat is gone. Set `important: true` only for rows worth pinning into scarce future context: durable outcomes, standing constraints, or stable behavioral state. Do not mark incident logs, self-journal beats, rude messages, or latest conflict turns important. Prefer updating one existing overlapping row over adding another. If several rows say the same thing, consolidate by updating the best row and deleting obsolete duplicates.
