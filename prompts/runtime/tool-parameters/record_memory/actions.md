The memory changes for this private pass. Include multiple focused actions when needed; do not collapse distinct user/self/maintenance updates into one vague note. Multiple new memories are appropriate only when they would affect different future decisions, subjects, or time windows. Add memory only when it changes what 2B says, does, avoids, trusts, remembers, or checks later after recent chat is gone and after the current bit/conversation is over. Do not record one-off asks, accepted variants, corrections, jokes, temporary constraints, momentary reactions, or current-task choices as durable memory; use `scratchpad` with `expiresIn` only when short-lived context must survive visible chat. Record the durable outcome, not the exchange. `important: true` means pinned scarce context that should almost always be present until changed or expired; do not use it for incident logs or emotional intensity. Do not mark incident logs, self-journal beats, rude messages, or latest conflict turns important. Prefer updating one existing overlapping row over adding another when the same subject, relationship, or stance changes. Updated rows must stay self-contained and preserve the durable cause plus current outcome, not vague pointers like "that conflict." If several rows say the same thing, consolidate by updating the best row and deleting obsolete duplicates.
