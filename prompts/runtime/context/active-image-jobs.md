Image generation is asynchronous. Active jobs keep typing while the worker runs. When ready, the runtime starts a normal 2B action loop with the generated image attached to the current turn and sends that message to the original request. Do not start a duplicate job for the same concrete request while a matching active job is visible here.
