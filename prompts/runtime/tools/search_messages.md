Privately search history when context is missing or prior messages matter. Modes: "semantic" for vague meaning, "literal" for exact words/errors/URLs/commands, "id" lookup, "context" around a result id or timestamp. For semantic, use a short topic phrase and put names/channels/times/bot-human constraints in filters. Defaults current channel; set `channel_id` for another accessible channel/thread or `guild_id` for broader guild search. `include_attachments` is slow; use only when filenames/types matter.
