Privately fetch external images by URL so 2B can inspect them before acting. Downloads, validates as image, resizes to a maximum edge of {{maxDimension}}px, converts to JPEG, and returns image data with metadata. Does not store the image; this is ephemeral fetch only. Use `read_asset` for typed chat-history assets. Maximum URLs per call: {{maxImagesPerCall}}.
