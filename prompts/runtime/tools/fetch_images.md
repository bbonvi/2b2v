Privately fetch external images by URL so 2B can inspect them before acting. Downloads and validates public images, resizes to a maximum edge of {{maxDimension}}px, and returns image data with its source URL and metadata; GIFs use a clearly marked static first-frame preview. Does not store the image. Use `read_asset` for typed chat-history assets. Maximum URLs per call: {{maxImagesPerCall}}.
