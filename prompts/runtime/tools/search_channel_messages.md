Privately search channel history when context is missing or prior messages matter. Modes: "literal" for exact words/errors/URLs/commands, "id" lookup, and "context" around a result id or timestamp. Use "id" to expand a trimmed visible MsgID. Defaults current channel; set `channel_id` for another accessible channel/thread or `guild_id` for broader guild search. `include_attachments` is slow; use only when filenames/types matter.
