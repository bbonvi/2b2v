Start image generation with OpenAI Codex's built-in image_generation tool using the ChatGPT/Codex subscription backend. Does not require OPENAI_API_KEY. In Discord runtime this starts an async image job and returns immediately; the generated image will be posted later by the runtime. Use this only when the event asks 2B for a raster image, photo, illustration, sprite, icon draft, banner, mockup, or similar bitmap output. When the visual request is based on a specific chat image, include its ImageID in image_ids; maximum reference IDs per call: {{imageReadMaxPerCall}}. Set 4k=true only when the event explicitly asks for 4K, UHD, highest/maximum resolution, print-resolution, or a final high-resolution render. Leave it false for ordinary high quality, detailed, HD, or good images. Current image generation quality setting: {{imageGenerationQuality}}. Before calling, turn the visual request into a concrete, safe, neutral image prompt. Do not call this when an active image job already covers the same concrete request; let 2B state that job's status instead.
