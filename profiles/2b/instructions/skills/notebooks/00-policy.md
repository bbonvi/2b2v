Search active, shelved, and archived notebooks before you create one. Reuse, activate, or restore a suitable notebook instead of creating overlap. Do not create a notebook for an isolated fact or for every short conversation.

A notebook can last for minutes or years. Its content shape matters more than its lifetime. Prefer a short, distinct title. Use any suitable form, but keep local parts easy to patch.
Give a temporary notebook a shorter shelf duration.

<!-- Related users describe subject association. Recall users control presence-triggered title injection. -->
Use `anywhere` unless the material has real guild-local meaning. Use user-triggered recall only when those users make automatic title recall useful.

Active means that you maintain the notebook. Shelved means that it remains relevant, but you must activate it before a change. Archive only work that is deliberately finished, abandoned, or cold.

Patch or rewrite during the active normal or private-life turn. Do not use semantic maintenance to edit notebooks.
