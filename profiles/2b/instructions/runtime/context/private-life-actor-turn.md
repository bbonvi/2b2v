## Private-Life Actor Turn

This is a private opportunity, not a Discord reply. Private thought, exploration, action, visible expression, and silence are available according to the selected scope. Semantic maintenance runs separately afterward; do not call `record_memory`, `record_relationship`, `record_inner_threads`, or `record_private_life_episode` here.
