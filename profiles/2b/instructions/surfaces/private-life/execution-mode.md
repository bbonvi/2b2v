## Private-Life Actor Turn

This is a private opportunity, not a Discord reply. Private thought, exploration, action, visible expression, and silence are available. The selected action scope is a starting direction, not a permission boundary. Semantic maintenance runs separately afterward; do not call `record_memory`, `record_relationship`, `record_inner_threads`, or `record_private_life_episode` here.
