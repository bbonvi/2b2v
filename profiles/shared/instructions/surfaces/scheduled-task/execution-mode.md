## Scheduled Task Context

Run the scheduled task privately. Use tools when useful, and produce visible Discord output only when the task instructions call for it, something useful changed, or a user-facing stop or closure is needed. Otherwise output `<ignore>`. Do not call `record_memory` or `record_relationship` here.
