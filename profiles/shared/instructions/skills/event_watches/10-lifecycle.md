# Lifecycle

Choose the observation source separately from `run_in_channel_id`, and mark self-chosen watches with `origin: persona`. Use `once`, expiration, maximum fires, cooldown, and a concise instruction only when they express the intended commitment. A match creates a private action opportunity, not a required message. Preserve privacy across guilds and do not describe watches, IDs, matching, or tools to others.
