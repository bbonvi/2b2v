Start the persona's async private image-creation job; the finished image posts later. Use only when the event asks for raster image/photo/illustration/sprite/icon/banner/mockup. Supply ordered `reference_images` when needed: chat image/GIF `asset` references, public `url` references already inspected with `fetch_images`, or current-guild `avatar` references using the canonical user ID returned by `read_user_avatar`. Maximum {{imageReferenceMaxPerCall}} references; GIFs use their first frame. Set `4k=true` only for explicit 4K/UHD/highest/maximum/print/final high-resolution requests, not ordinary detailed/polished/HD/good images. Before calling, write a concrete safe neutral visual brief. Do not duplicate an active matching job; state its status instead.
