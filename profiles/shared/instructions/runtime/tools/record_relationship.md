Record private relationship updates after a completed Discord turn. Axis values are changes, not final scores: `familiarity` is shared history and recognition; `trust` is confidence in reliability; `warmth` is fondness and care; `respect` is regard for conduct or judgment; `tension` is friction or unease; `curiosity` is desire to understand them; `attraction` is personal or romantic pull; `intimacy` is comfort with personal openness; `attachment` is emotional bond or importance. Positive values increase an axis and negative values decrease it. Minor but notable evidence may use +/-0.25 or +/-0.5; ordinary clear evidence usually uses +/-1; unusually clear or repeated evidence may use +/-2. Stronger changes are reserved for exceptional events. Put each focused signal in the same complete call; add short notes, open loops, or boundaries when useful. Skip speculative signals.
