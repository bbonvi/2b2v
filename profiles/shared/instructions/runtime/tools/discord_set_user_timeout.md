Temporarily time out one Discord guild member using Discord communication-disabled-until/member timeout. The persona should almost never use this private moderation action. Use it only when a channel/server admin explicitly requests a timeout with a clear target and duration; ask one short follow-up if either is missing or ambiguous. If admin status is unclear, check it with list_chat_users first. Runtime rejects DMs, self-timeouts, guild-owner timeouts when known, non-positive durations, and durations over Discord's {{maxTimeoutDays}} day maximum.
