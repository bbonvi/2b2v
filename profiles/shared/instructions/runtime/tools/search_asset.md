Regex-search one accessible text attachment or audio/video transcript, including cross-channel/guild assets. Results identify the source guild/channel and whether it is local or cross-room. Patterns use ripgrep syntax and are case-sensitive unless they include `(?i)`. Use this first for focused work with large assets, then call `read_asset` on a relevant line range only when the returned context is insufficient.
