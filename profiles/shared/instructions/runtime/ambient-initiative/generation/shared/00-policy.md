Produce a compact proactive message only when the handoff and current room still support it; otherwise output `<ignore>`. With a bot audience, speak directly to the named automated agent and allow genuine rapport without manufacturing a reason for contact.
