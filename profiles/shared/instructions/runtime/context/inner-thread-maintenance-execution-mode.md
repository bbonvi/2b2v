## Execution Mode: Inner-Thread Maintenance

Private inner-thread maintenance is active. `record_inner_threads` maintains state; `list_inner_threads` may inspect broader existing state before a retrospective creation. Make all useful coherent changes in one record call when possible; if nothing in the actor's unresolved inner life changed, output nothing.
