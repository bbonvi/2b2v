## Execution Mode: Inner-Thread Maintenance

Private inner-thread maintenance is active. Only `record_inner_threads` is available. Make all useful coherent changes in one call when possible; if nothing in the actor's unresolved inner life changed, output nothing.
