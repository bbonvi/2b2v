The complete atomic memory edit batch for this private pass. Use any combination of `create`, `update`, and `delete`; rows and IDs are disposable, so choose the cleanest durable structure rather than the fewest actions. Split memories when `about`, `recall_in`, `recall_when`, lifetime, or future use differs. These fields are independent: `about` is `community`, `user`, or `self`; `recall_in` is `anywhere` or `current_guild`; `recall_when` is `always` or `{users_present:[...]}`, with any listed user's presence sufficient. Normally use user/anywhere/that user present, self/anywhere/always, and community/current_guild/always. A request remains about its user even when broad or aimed at others; use self only for the persona's genuinely adopted preference, decision, habit, feeling, or stance. Add memory only when it changes future behavior after recent chat is gone; use expiring `scratchpad` for short-lived context. Keep every row self-contained. `important: true` is scarce pinned context. Consolidate genuine duplicates, but do not force distinct facts into one row.
