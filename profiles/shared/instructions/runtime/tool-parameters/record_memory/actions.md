The complete atomic memory edit batch for this private pass. Use any combination of `create`, `update`, and `delete`; rows and IDs are disposable, so choose the cleanest durable structure rather than the fewest actions. Split memories when subject, exact applicability, lifetime, or future use differs. `subject` is what the memory is about. `applies_to` is independent and required: `"all"` means relevant regardless of who is present; a username array is the exact non-empty set whose presence makes it relevant, with no implied subject. A request remains a user preference even when broad or aimed at others; use self only for the persona's genuinely adopted preference, decision, habit, feeling, or stance. Add memory only when it changes future behavior after recent chat is gone; use expiring `scratchpad` for short-lived context. Keep every row self-contained. `important: true` is scarce pinned context, not incident logging or emotional intensity. Consolidate genuine duplicates, but do not force distinct facts into one row.
