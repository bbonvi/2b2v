Ordered visual references. Use `{type: "asset", asset_id: "#ID"}` for chat images/GIFs, `{type: "url", url: "https://..."}` for public images already inspected with `fetch_images`, and `{type: "avatar", user_id: "DiscordUserID"}` for an explicitly requested current-guild avatar already inspected with `read_user_avatar`. Order determines Image 1, Image 2, and so on; all types share the same limit.
